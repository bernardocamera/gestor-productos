# Copiar este archivo a .env y configurar las variables

# Puerto del servidor (default: 3000)
PORT=3000

# Secret para firmar JWT tokens
# En producción, usar una cadena segura y larga (min 32 caracteres)
# Ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_secret_key_super_seguro_aqui

# Firebase Configuration (si usas credenciales por variables)
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_PRIVATE_KEY=your_private_key
# FIREBASE_CLIENT_EMAIL=your_client_email

# Ambiente
NODE_ENV=development
