# Copiar este archivo a .env y configurar las variables

# Puerto del servidor (default: 3000)
PORT=3000

# Secret para firmar JWT tokens
# En producción, usar una cadena segura y larga (min 32 caracteres)
# Ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_secret_key_super_seguro_aqui

# Firebase Configuration - Opciones:
# OPCION 1: Base64 del JSON (recomendado para Vercel)
#   - Generar con: $json = Get-Content -Raw "src/config/firebaseKey.json"; [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($json))
#   - Luego en Vercel: vercel secrets add firebase_key_base64 <base64_string>
FIREBASE_KEY_BASE64=

# OPCION 2: Archivo local
#   - Coloca src/config/firebaseKey.json o src/config/firebaseKey.json.local (no versionado en git)
#   - data.js lo buscará automáticamente

# Ambiente
NODE_ENV=development
