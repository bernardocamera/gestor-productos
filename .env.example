# Copiar este archivo a .env y configurar las variables

# Puerto del servidor (default: 3000)
PORT=3000

# Secret para firmar JWT tokens
# En producción, usar una cadena segura y larga (min 32 caracteres)
# Ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_secret_key_super_seguro_aqui

# Firebase Configuration
# OPCION 1: Base64 del JSON completo de la cuenta de servicio (recomendado para Vercel)
#   - Generar con: $json = Get-Content -Raw "src/config/firebaseKey.json"; [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($json))
#   - Luego en Vercel: Settings > Environment Variables > Add FIREBASE_KEY_BASE64
FIREBASE_KEY_BASE64=

# OPCION 2: Variables individuales (fallback para debugging)
#   - Extrae estos datos del JSON de tu cuenta de servicio de Firebase
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY_ID=

# OPCION 3: Archivo local (para desarrollo)
#   - Coloca src/config/firebaseKey.json o src/config/firebaseKey.json.local (no versionado)
#   - data.js lo buscará automáticamente

# Ambiente
NODE_ENV=development
